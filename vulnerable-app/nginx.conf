server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routes (react-router) must all serve the app shell.
    location / {
        try_files $uri /index.html;
    }
}
